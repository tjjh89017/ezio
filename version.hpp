#ifndef __VERSION_HPP__
#define __VERSION_HPP__

// Fallback version for IDE
// When compiling, CMake will define EZIO_VERSION via target_compile_definitions
#ifndef EZIO_VERSION
#	define EZIO_VERSION "dev"
#endif

#endif
